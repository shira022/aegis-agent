{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Aegis Agent main window: core APIs only, no shell plugin access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
