name: Path Leak Guard

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  scan:
    name: Scan for leaked paths
    # TODO: switch the reusable workflow ref from @feature/path-leak-guard to @main
    # once shira022/loop-engineering-template#52 is merged.
    uses: shira022/loop-engineering-template/.github/workflows/path-leak-check.yml@feature/path-leak-guard
